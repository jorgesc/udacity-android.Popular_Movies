<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.udacity_project_1.popularmovies">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.example.udacity_project_1.popularmovies" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MovieDetails"
            android:parentActivityName=".MainActivity"
            >

        </activity>

        <provider
            android:authorities="com.example.udacity_project_1.popularmovies"
            android:name="com.example.udacity_project_1.popularmovies.utils.FavoriteContentProvider"
            android:exported="false" />

    </application>

</manifest>